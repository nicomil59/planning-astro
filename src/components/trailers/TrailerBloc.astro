---
import { YouTube } from '@astro-community/astro-embed-youtube';

const { titre, plateforme, videoId } = Astro.props;
---

<div class="my-8">
    <h3 class="text-lg sm:text-xl mb-2"><span class="font-semibold">{titre}</span> sur {plateforme}</h3>
    <YouTube id={videoId} />
</div>