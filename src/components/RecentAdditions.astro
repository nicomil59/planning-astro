---
import recentAdditions from "../data/recentAdditions.json";

const updateDate = "30 septembre 2023";
---

<section id="recent" class="my-8">
  <h2 class="font-bold text-2xl sm:text-4xl my-4 border-b-4 sm:border-b-8 border-notionyellow-600 inline">
    Derniers ajouts
  </h2>
  <p class="my-8 text-sm sm:text-lg font-semibold text-center rounded bg-notionyellow-50 sm:w-80 m-auto px-2 py-1">
    Mise à jour : <span>{updateDate}</span>
  </p>
  <p>Les dernières dates ajoutées au planning.</p>

  <table class="my-4 text-xs sm:text-sm table-auto max-w-screen-md sm:w-4/5 m-auto border-separate border-spacing-1">
    <thead>
      <tr>
        <th class="bg-notionyellow-100 p-2">Date</th>
        <th class="bg-notionyellow-100 p-2">Titre</th>
        <th class="bg-notionyellow-100 p-2">Saison</th>
        <th class="bg-notionyellow-100 p-2">Plateforme</th>
      </tr>
    </thead>
    <tbody>
      {
        recentAdditions.map((item) => (
          <tr class="bg-notiongray-100 hover:bg-gray-50">
            <td class="font-semibold p-1">
              {item.Date.split("/").slice(0, 2).join("/")}
            </td>
            <td class="p-1 font-medium">{item.Titre}</td>
            <td class="p-1">{item.Saison}</td>
            <td class="p-1">{item.Plateforme}</td>
          </tr>
        ))
      }
    </tbody>
  </table>
</section>